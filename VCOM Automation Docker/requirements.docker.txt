# ============================================================
# SCADA VCOM Automation — Pinned Docker Dependencies
# ============================================================
# This file replaces the unpinned requirements.txt for Docker
# builds. Pin versions are tested together and should only be
# updated deliberately, not on every build.
# ============================================================

# setuptools MUST be first — amqtt and other packages import
# pkg_resources (a setuptools module) at import time. Python
# 3.12-slim does not ship setuptools, so it must be installed
# before any package that depends on it.
setuptools>=69.0.0

# Data processing
pandas==2.2.3
numpy==1.26.4
openpyxl==3.1.5

# File system monitoring (watchdog analysis engine)
watchdog==4.0.2

# Browser automation (VCOM scraper)
playwright==1.48.0

# Web framework (dashboard)
fastapi==0.115.0
uvicorn[standard]==0.31.0
jinja2==3.1.4
python-multipart==0.0.12

# Networking & integrations
requests==2.32.3

# MQTT (tracker heartbeat)
# paho-mqtt 2.x imports errno.WSAEWOULDBLOCK (Windows-only) unconditionally on Linux — use 1.6.1
paho-mqtt==1.6.1
amqtt==0.11.0b1

# Process management (used by dashboard_doctor and run_monitor)
psutil==6.1.0

# sqlite3 is part of the Python stdlib — no install needed
