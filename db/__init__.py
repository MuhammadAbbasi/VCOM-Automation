# db/ — SQLite database layer for Mazara SCADA monitoring system.
#
# Two databases:
#   scada_data.db  — All extracted SCADA measurements + analysis snapshots
#   scada_logs.db  — All application logs (extraction, watchdog, dashboard)
